const path = require('path');
require('dotenv').config({ path: path.join(__dirname, '../.env') });
// console.log("All environment variables:", process.env);
// console.log("\nAudioShake API Key:", process.env.AUDIOSHAKE_API_KEY);
